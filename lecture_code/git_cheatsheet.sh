# clone a git repository
git clone https://github.com/cmsc330fall23/cmsc330fall23.git

# add a file to be tracked
git add README.md

# take a snapshot of the repository
git commit -m "commented about what has been changed"

# push your changes to github
git push

# check what has changed ot what needs too be done to push
git status

# see a log of past changes
git log

# pull changes on github to local machine
git pull
